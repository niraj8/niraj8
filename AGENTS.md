# Notes

## Games

Games are deployed from this repo but developed in `../games` (separate repo).
When updating a game, copy the built files from `../games/<game-name>` into the corresponding directory here.
